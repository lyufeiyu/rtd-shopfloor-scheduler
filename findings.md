# 检查发现

- 当前工作区新增 algorithm 目录，需要先确认其内容，不能沿用之前外部算法路径。
- 现有前端是静态 data.js 与浏览器内规则模拟，缺少上传和后端执行链路。
- 删除目标已确认：frontend/preview-desktop.png、preview-dispatch.png、preview-mobile.png、preview-theme.png。
- 本次整体视觉重新设计，不读取这些预览作为设计参考。

## 实际数据与处理口径

- `algorithm/RTD_Dataset_v6.xlsx` 实际包含 9 张表，没有单独的“工站-设备表”；系统从“5-工艺路线-设备表”重建工站映射并给出 warning。
- 设备表 908 行、设备名去重后 907 台；`DB-72` 两条记录约束冲突，已标记“数据冲突”并禁用，避免错误派工。
- WIP 319 条；制造单批次流转 1860 条，其中 669 条没有下一工站、275 条与 WIP 重叠，二者不进入本次待排，最终待排 916 条。
- 未注册的路线设备、未知芯片尺寸、未登记设备等均保留为 warning 或未排原因，不静默丢失。

## 已验证链路

- 上传、元数据概况、WF 工站快速排产、结果查询和 `summary.xlsx` 下载已通过接口级闭环；结果为 250 安排、90 未安排、0 violations。
- 前端通过轮询任务状态自动进入结果页，算法源码和 worker 日志没有公开接口。
- 初始页面曾自动选择服务端最新数据集，导致未上传时出现统计；现已改为本次浏览器会话空白启动，上传成功后才填充数据。
- 停止脚本曾只按 PID 文件停止，无法覆盖旧的直接启动进程；现增加按本项目 `backend/server.py` 路径的兼容清理，并加入前端服务心跳。
- 上传返回 `formatCheck`：检查工作表、必需字段和数据质量；结构性错误直接拒绝上传，warning 则在数据概况中展示。
