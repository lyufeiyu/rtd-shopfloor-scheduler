# RTD 产品化与三人协作计划

## 目标
重新设计产品界面，删除 preview-*.png，完成上传数据集→后台排产→结果查看/下载的真实闭环，算法代码与日志不暴露给前端，并提供三人可独立合并的任务安排。

## 阶段
1. [x] 检查当前 algorithm 输入输出与数据处理风险，固定接口。
2. [x] 实现后端上传、校验、异步任务、日志与受控结果下载。
3. [x] 重做前端视觉和产品流程，联通设备/结果视图。
4. [x] 实际上传与运行验证、静态脚本检查、Codex 只读审查准备。
5. [x] 交付运行说明、接口契约及三人任务安排。

## 验收记录

- 实际上传 `algorithm/RTD_Dataset_v6.xlsx`：9 张表、907 台设备、319 条 WIP、916 条待排任务、6 项数据提示。
- WF 工站快速排产：250 条已安排、90 条暂未安排、0 项约束冲突，成功下载 `summary.xlsx`。
- `python -m py_compile algorithm/*.py backend/server.py`、`node --check frontend/app.js` 通过。
- 浏览器 smoke test 已保留在 `backend/check-product.cjs`；本机 Chrome headless 受系统进程限制时，使用上述接口级闭环作为可复现验收。
- 修正启动/停止语义：`停止RTD.command` 会清理 PID，并兼容清理旧的本项目 `server.py` 进程；前端心跳发现服务停止后清空当前数据。
- 初始页面不再读取服务端历史数据；必须在当前浏览器会话上传并通过格式检查后才展示设备、WIP 和待排统计。

## 约定
- 前端只展示业务状态，不展示算法源码、训练细节或服务器日志。
- 本地演示服务默认只监听 127.0.0.1；所有输出按运行 ID 隔离。
- 历史参考 RTD_V3 不修改；使用当前工作区 algorithm 目录。
- 以真实数据端到端运行与三模块接口一致为验收条件。
